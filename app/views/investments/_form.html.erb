<%= form_for investment do |f| %>
    <%= f.label "Amount" %>
    <%= f.number_field :amount %>
    <%= f.collection_select :cryptocoin_id, Cryptocoin.all, :id, :name %>
    <%= f.submit %>
  <% end %>