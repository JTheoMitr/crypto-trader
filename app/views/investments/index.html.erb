<%= flash[:message] %>

<%= display_header(@cryptocoin) %>

<ul> <% @investments.each do |inv| %>
$<%= link_to(inv.amount, investment_path(inv)) %> of <%= inv.cryptocoin.name %> on <%= inv.created_at.strftime("%B %-d, %Y at %H:%M %p") %> </br>
<% end %></ul>