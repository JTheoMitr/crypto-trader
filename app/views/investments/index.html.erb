<%= flash[:message] %>

<%= display_header(@cryptocoin) %>

<ul> <% @investments.each do |inv| %>
$<%= inv.amount %> of <%= inv.cryptocoin.name %> 
on <%= inv.readable_time %> yielding <%= (inv.amount / inv.cryptocoin.dollar_value) %> <%= inv.cryptocoin.name %>
<%= link_to("View Order", investment_path(inv)) %>  </br>
<% end %></ul>